# network-packet-analyzer
Developed a low-level packet sniffing tool in C++ using Raw Sockets to intercept and analyze real-time network traffic at the Data Link Layer.  Implemented custom parsing logic for Ethernet, IP, TCP, and UDP headers by mapping raw byte streams to protocol-specific data structures.  Configured Promiscuous Mode on Linux network interface
